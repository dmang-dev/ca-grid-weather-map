# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Fetched data is committed for the GitHub Pages demo snapshot.
# Re-run `python fetch_data.py` locally to refresh; the dirty diff is
# expected (commit it to update the demo, or `git restore data/` to revert).

# Logs and scratch
*.log
.rstk/
.claude/
probe_endpoints.py

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.idea/
.vscode/
*.swp
